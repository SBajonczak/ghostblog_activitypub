const axios = require('axios');
const config = require('./consts');


exports.HandleAddTxt = async function (slug) {
    try {
        let result = "";
        return result;
    } catch (error) {
        console.error('Error fetching author:', error.message);
    }
}
